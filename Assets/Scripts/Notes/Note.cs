namespace aprilJam
{
  // Значения только в дипозоне от 0 до 9,
  // т.к. значения Note преобразуются в числовую строку в NoteCombination
  public enum Note : byte
  {
    Do  = 0,
    Re  = 1,
    Mi  = 2,
    Fa  = 3,
    Sol = 4,
    La  = 5,
    Si  = 6
  }
}
